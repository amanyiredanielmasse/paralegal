[project]
name = "paralegal-sophia"
version = "0.1.0"
description = "Sophia - Ugandan Paralegal AI Assistant"
authors = [
    {name = "Your Name", email = "your@email.com"},
]
dependencies = [
    "strands-agents>=1.0.0",
    "strands-agents-tools>=1.0.0",
    "boto3>=1.34.0",
    "python-dotenv>=1.0.0",
    "fastapi>=0.111.0",
    "uvicorn>=0.30.0",
    "mangum>=0.17.0",
    "supabase>=2.4.0",
    "python-docx>=1.1.0",
    "httpx>=0.27.0",
    "pydantic>=2.0.0",
    "tavily-python>=0.3.0",
    "pypdf>=4.0.0",
]
requires-python = ">=3.11,<3.13"

[dependency-groups]
dev = [
    "pytest>=9.0.2,<10.0.0",
    "pytest-asyncio>=1.0.0,<2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.pytest.ini_options]
pythonpath = "."
asyncio_default_fixture_loop_scope = "function"
